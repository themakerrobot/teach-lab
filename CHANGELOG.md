# 바뀐 것

번호는 둘로 나뉩니다. **웹앱**(사이트 · `TeachLab.exe`)은 `v0.1.1` 처럼,
**파이썬 패키지**(`pip install teachlab`)는 `0.4.0` 처럼 따로 셉니다.
둘은 서로 독립이라 같은 날 둘 다 올라가도 번호가 다를 수 있어요.
규칙은 [DEVELOP.md의 버전·태그·릴리스](DEVELOP.md#버전태그릴리스) 에 있습니다.

## 웹앱

### 아직 안 올린 것 (main)

- 학습실: 예시를 모을 때마다 목록 전체를 다시 그리던 것을 고쳐 200장 가까이 모아도 느려지지 않아요
- 학습실: 200장이 차면 꾹 누르고 있어도 멈추고 한 번만 알려요. 배우는 동안은 모으기 버튼이 잠겨요
- 학습실: 이미지 미리보기가 끊기지 않아요 (특징 뽑기만 11Hz)
- 시험실: 사진 파일로 맞힌 답을 카메라가 바로 덮어쓰지 않아요
- 시험실: 손·얼굴·포즈가 아직 안 잡힐 때 "카메라를 켜 주세요" 대신 "아직 안 보여요"
- 보관함 안내 문구 · 설명서 그림 · README 새로 씀

### v0.1.1 — 2026-09-17

- 소리 정확도: YAMNet 한 창(15600 샘플)이 아니라 1초를 넣고 뒤쪽 무음 창을 쓰던 버그.
  소리 모델은 **다시 가르쳐야** 제대로 나옵니다 (옛 모델도 열리긴 해요)
- 소리 특징을 `sqrt + L2` 로 다듬어 비슷한 소리끼리 더 잘 갈려요
- 파이썬 내보내기가 배포된 사이트에서 404 로 실패하던 것 (라이브러리 소스를 JS 안에 심음)

### v0.1.0 — 2026-09-17

첫 공개.

- 이미지 · 손(한 손/두 손) · 얼굴 · 포즈(상반신/전신) · 소리, 다섯 가지 소스
- 학습실 → 시험실 → 보관함, 튜토리얼(파이보), 한/영 토글
- 프로젝트 파일(`.teachlab.zip`) 내보내기 · 불러오기, 이어서 배우기
- 파이썬 내보내기 (모델 + 특징 모델 + 실행 코드)
- 인터넷 없이 쓰는 Windows 용 `TeachLab.exe`

## 파이썬 패키지 `teachlab`

### 0.4.0 — 2026-09-17

- 소리: 창 길이 15600 샘플, `sqrt + L2` 변환 (웹앱 v0.1.1 과 같은 계산). `project.json` 의
  `featureTransform` 을 읽어 옛 모델은 날것으로 계산해요
- `teachlab listen` 도 같은 창 길이

### 0.3.1 — 2026-09-17

- Windows 한글 경로(`바탕 화면` 등)에서 모델이 안 열리던 것

### 0.3.0 — 2026-09-17

- PyPI 첫 공개. 이미지 모델은 LiteRT 로 돌려서 TensorFlow · MediaPipe 없이 약 110MB
- 손 · 얼굴 · 포즈 · 소리는 `teachlab[landmark]` · `teachlab[sound]` 로 MediaPipe 를 따로 받음
- `teachlab info / predict / webcam / listen` 명령
